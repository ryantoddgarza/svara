import React from 'react';
import Layout from '../utils/Layout';
import Patch from '../components/organisms/Patch';

export default () => {
  return (
    <Layout>
      <Patch />
    </Layout>
  );
};

import styled from 'styled-components';
import { Container } from '@svara/ui';

export const Player = styled.div`
  position: fixed;
  bottom: 0;
  width: 100%;
  background: white;
  border-top: 1px solid #757575;
  z-index: 6000;
`;

export const PlayerRow = styled(Container)``;
